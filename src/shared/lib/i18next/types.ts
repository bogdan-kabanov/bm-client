export interface TranslationKeys {
  welcome: string;
  about: string;
  contact: string;
  home: {
    title: string;
    subtitle: string;
  };
  buttons: {
    submit: string;
    cancel: string;
  };
}

export type Language = 'en' | 'ru';